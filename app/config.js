const API_BASE_URL = import.meta.env.VITE_API_BASE_URL;
// currently tbd, so just putting it here so we can change it later
const PRODUCT_NAME = '497 Language Project'

export {API_BASE_URL, PRODUCT_NAME};